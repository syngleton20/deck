/* Initialization */
window.addEventListener('touchstart', evt => { }, { passive: true });

window.addEventListener("load", function () {
    setTimeout(function () {
        window.scrollTo(0, 1);
    }, 0);
});